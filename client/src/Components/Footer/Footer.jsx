import React from "react";

function Footer() {
    return (
        <footer className="Layout-footer">
            © {new Date().getFullYear()} Daniel Kiss PA work.
        </footer>
    );
}

export default Footer;